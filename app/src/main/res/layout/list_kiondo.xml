<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/list_row_selector"
    android:padding="8dp" >

    <!-- Add icon -->
  <!--  <ImageView
        android:id="@+id/add_icon"
        android:layout_width="15dp"
        android:layout_height="15dp"
        android:gravity="center"
        android:layout_centerVertical="true"
        android:layout_alignParentLeft="true"
        android:layout_marginRight="8dp"
        android:background="@drawable/plus"/>-->

    <!-- Recipe Title -->
    <TextView
        android:id="@+id/kiondo_ingredient"
        android:text="Kiondo Ingredient"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        android:textSize="@dimen/recipe_title"
        android:textStyle="bold"
        android:fontFamily="@font/aileron_light"/>

    <TextView
        android:id="@+id/recipe_title_kiondo"
        android:text="Recipe Title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/kiondo_ingredient"
        android:textSize="14sp"
        android:textStyle="bold"
        android:fontFamily="@font/aileron_light"/>

</RelativeLayout>